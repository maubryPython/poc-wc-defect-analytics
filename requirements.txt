# Nike WC Defect Analytics — POC Dependencies
# Install: pip install -r requirements.txt
#
# Databricks CE runtime already includes: pyspark, pandas, numpy, scikit-learn, mlflow
# Install these additionally in your Databricks cluster init script or %pip magic:

# Agent framework
langgraph>=0.1.0
langchain>=0.2.0
langchain-community>=0.2.0

# LLM (HuggingFace Inference API — no local GPU required)
huggingface-hub>=0.23.0
requests>=2.31.0

# Data quality
great-expectations>=0.18.0

# dbt (SQL transformation layer)
dbt-core>=1.8.0
dbt-spark>=1.8.0          # Databricks adapter

# Data generation (dev only)
faker>=25.0.0
openpyxl>=3.1.0           # Excel read/write

# Utilities
pyyaml>=6.0
python-dotenv>=1.0.0      # For local .env with HuggingFace token

# Visualization data export
tabulate>=0.9.0
